<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="sbtraxapp.htotten.github.com.sbtrax">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_bootprint"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_bootprint"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Trail5Activity"></activity>
        <activity android:name=".Trail4Activity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Trail3Activity" />
        <activity android:name=".EmergenciesActivity" />
        <activity android:name=".QuestionsActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".Trail1Activity"
            android:label="@string/app_name" />
        <activity android:name=".Trail2Activity" />
    </application>

</manifest>